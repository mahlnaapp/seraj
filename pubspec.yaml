name: seraj
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.1
  appwrite: ^17.1.0
  shared_preferences: ^2.2.3
  uuid: ^4.3.3
  geolocator: ^14.0.2       # تحديث لأحدث إصدار متوافق مع NDK 27
  geocoding: ^4.0.0      # تحديث إلى أحدث إصدار
  flutter_web_auth_2: ^4.1.0 # تأكد من إبقاءها لأنها تعتمد على NDK 27

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0       # تحديث لآخر إصدار
  flutter_launcher_icons: "^0.14.4"  # تحديث لأحدث إصدار

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icon.png"
  min_sdk_android: 21
  remove_alpha_ios: true
